2.0.6

- 优化非译元素正则生成：内部新增 token 安全上限，避免单次请求过大导致模型返回不完整或校验失败。
- 正则生成阶段按非译元素类型、用户字符上限、内部 token 上限和元素数量自动分批，不再把大量缺失元素一次性发送给模型。
- 正则生成纠偏重试改为优先只重发失败条目，已成功生成的规则会保留，减少重复请求和 token 消耗。
- 修复发布包中内置规则库文件缺失时可能导致任务失败的问题；样例回写失败会降级为警告，不影响结果导出。
- 增强任务诊断日志，失败时会在任务日志目录保留完整请求、完整响应和本地校验问题，方便定位模型返回错误。
