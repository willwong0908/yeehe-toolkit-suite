2.0.5

- 新增 AI 审校工具，支持 Excel / XLIFF 导入、列映射、普通审校、定向审校、禁用词检查、任务日志与结果导出。
- 审校设置新增单次请求字符上限，可单独控制 AI 审校每次发送给模型的文本长度。
- 新增工具说明默认页，打开 WebUI 后先展示已可用工具；点击工具卡片可查看简洁使用说明。
- 优化发布版启动体验，控制台显示更清晰，浏览器会在服务启动后自动打开，并支持在控制台输入 q 关闭。
- 应用品牌与发布包命名统一为 Yeehe / 译禾工具合集，并继续支持自动更新检查。
